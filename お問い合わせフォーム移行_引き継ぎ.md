# お問い合わせをGoogleフォームからサイト内フォームに移行する（引き継ぎ）

作成日: 2026-08-15
依頼元: ユーザー（japan-proxy-cost で同じ移行を実施済み。その方式に揃える）

---

## 1. 何をするのか

いまの `/privacy`（`/en/privacy`）は、お問い合わせを**外部のGoogleフォームへのリンク**で
済ませている。これを **サイト内に置いた実物の `<form>`** に差し替える。送信の中継には
[FormSubmit](https://formsubmit.co/) を使う（バックエンドを持たない方針を維持するため）。

### なぜ変えるのか

- 外部リンクだと、押した時点でサイトを離れる。連絡手段としては動くが、審査の観点では
  「サイト内に連絡手段が無い」と見られる余地が残る
- japan-proxy-cost が 2026-08-14 に同方式へ移行済み。**3アプリで送信先を1つに揃えると管理が楽**
  （届いたメールの件名で発信元アプリを見分ける）

### このアプリ固有の注意: 日英2言語

`src/staticPages.js` の `CONTACT_FORM_URL` は駅前スコア（eki-facility-app）と共用している。
移行はアプリごとに独立して行う（送信先エンドポイントだけが共通）。

**フォームのラベルは `src/locales/ja.json` / `en.json` の両方に足す。**
既存の `privacyPage.contact*` キーの並びに揃えること。

---

## 2. 送信先（すでに有効化済み。新たな登録は不要）

```
https://formsubmit.co/21c6f56659051072bab367d0af9fb0bc
```

- この文字列は kakuniadao7@gmail.com のエイリアス（ハッシュ）。**メールアドレスそのものはHTMLに出ない**
- japan-proxy-cost で 2026-08-14 に有効化済み。**再有効化の操作をしてはいけない**
  （FormSubmitは新しい有効化リクエストを出すと前のトークンが無効になる。
  japan-proxy-cost のフォームが止まる）

### ⚠ 着手して最初にやること: 疎通確認

同じエイリアスを**別ドメインから**使えるかは未検証。FormSubmitは既定ではドメイン制限を
かけないはずだが、確認せずに本番へ出さないこと。

1. ローカルビルドの `/privacy` からテスト送信を1通
2. kakuniadao7@gmail.com に届くか確認
3. 届かない・エラーになる場合は、**そこで止めてユーザーに報告する**。
   勝手に有効化リクエストを送ると japan-proxy-cost 側が巻き添えで止まる

### 2026-08-15の実測でわかったこと（上記の前提が2つ崩れた）

**① 有効化はフォームを設置するURLごとに必要。**
エイリアス文字列（`21c6f...`）はメールアドレス単位で共通だが、**新しいURLから最初に送信すると
メールは届かず、代わりに「Action Required: Activate FormSubmit on <そのURL>」が届く**。
`/privacy` と `/en/privacy` で別々の有効化メールが来たため、単位はドメインではなくURLと見られる。

→ **本番デプロイ後、`https://locker.kakuni-lab.com/privacy` と `/en/privacy` の
それぞれで1通ずつ送信し、届いた有効化メールを押す必要がある。**
これを踏むまで、公開されたフォームは利用者から見て無言で失敗する。
**この作業が終わるまでデプロイしないこと**（現行のGoogleフォームのリンクは動いているので、
中途半端に差し替える方が悪化する）。

#### ①の裏付け（同じく2026-08-15、ハブサイト公開時）

`https://kakuni-lab.com/` にフォームを設置して初回送信したところ、**やはり有効化メールが届き、
それを押してから再送信して初めて本文が届いた。**
localhostで既に有効化済みだったにもかかわらず必要だったので、
**単位はエイリアスではなくURL（少なくともドメイン）で確定。**

#### この制約の運用方法（デプロイとの順序）

「有効化してからデプロイ」は**原理的に不可能**。有効化リクエストはそのURLに設置された
フォームから送信して初めて発生するので、公開が先になる。したがって実際にはこう進める。

1. デプロイする
2. **すぐに**本番の `/privacy` と `/en/privacy` から1通ずつ送る
3. 届いた有効化メール2通の「ACTIVATE FORM」を押す
4. もう一度送って実際に届くことを確認する

1〜3の間（数分）は利用者から見てフォームが失敗する。ここは避けられないので、
**デプロイしたら他の作業を挟まずに2〜4まで一気にやること。**

**② japan-proxy-cost は巻き添えになっていない。**
localhost用の有効化リクエストを出した後も、japanproxy宛の再有効化依頼は発生しなかった。
有効化がURL単位で独立しているため、§5の懸念は当たらない。ただし他アプリのURLから
有効化リクエストを出す操作は引き続き避けること。

**③ FormSubmit側が不安定なことがある。**
2026-08-15の検証中、送信が HTTP 500 を返し、有効化の確認ページも "Server Error" になった。
コード側の問題と切り分けるため、500が出たら時間を空けて再試行する。

---

## 3. 実装（このリポジトリの構造に合わせた手順）

### 3-1. 大前提: Reactとプリレンダの両方に足すこと

固定ページは **`src/pages/PrivacyPage.jsx`（React）と `scripts/prerender.js` の
`privacyPage(lang)`（静的HTML）が、同じ翻訳キーから別々に組み立てる**構造になっている。

**片方だけに足すと、静的HTMLと画面の中身がズレる。** `createRoot` は `#root` の中身を
丸ごと捨てて描き直すので、プリレンダ側にだけフォームを足しても、ブラウザで開いた瞬間に消える。

→ **必ず2ファイル同時に**直す。

### 3-2. `frontend/src/staticPages.js`

`CONTACT_FORM_URL`（Googleフォーム）を削除し、FormSubmitの送信先に置き換える。
`hasContactForm()` はそのまま残してよい（判定対象を差し替えるだけ）。

```js
// お問い合わせフォームの送信先（FormSubmit）。メールアドレスのエイリアスで、
// 生のアドレスはHTMLに出ない。japan-proxy-cost・駅前スコアと同じ送信先を共用し、
// 件名（_subject）で発信元アプリを見分ける。
// ここを空文字にするとお問い合わせ欄ごと出さない（動かないフォームは置かない方針）。
export const CONTACT_FORM_ENDPOINT =
  "https://formsubmit.co/21c6f56659051072bab367d0af9fb0bc";

export function hasContactForm() {
  return CONTACT_FORM_ENDPOINT.length > 0;
}

export function pathForContactReceived(lang) {
  return `${langPrefix(lang)}/contact-received`;
}
```

### 3-3. フォームの中身

`_subject` と `_next` 以外は日英で共通。ラベルは `t()` で引く。

```html
<form class="contact-form" action="{CONTACT_FORM_ENDPOINT}" method="POST">
  <input type="hidden" name="_subject" value="池袋ロッカーアプリ — お問い合わせ" />
  <input type="hidden" name="_captcha" value="false" />
  <input type="hidden" name="_template" value="table" />
  <input type="hidden" name="_next" value="{SITE_URL}{pathForContactReceived(lang)}" />
  <!-- ボット除け。人間には見えない欄で、埋まっていたら送信を捨てる -->
  <input type="text" name="_honey" style="display:none" tabindex="-1" autocomplete="off" />

  <label><span>{t("privacyPage.formTopic")}</span>
    <select name="種類" required>… ロッカー情報の誤り / 駅の追加リクエスト / 削除依頼 / その他 …</select>
  </label>
  <label><span>{t("privacyPage.formPage")}</span>
    <input type="url" name="該当ページ" /></label>
  <label><span>{t("privacyPage.formDetails")}</span>
    <textarea name="内容" rows="6" required></textarea></label>
  <label><span>{t("privacyPage.formSource")}</span>
    <input type="url" name="参照元" /></label>
  <label><span>{t("privacyPage.formEmail")}</span>
    <input type="email" name="email" /></label>

  <button type="submit">{t("privacyPage.formSubmit")}</button>
</form>
```

ポイント:

- **`_subject` はアプリごとに必ず変えること。** 3アプリの問い合わせが同じ受信箱に届くので、
  件名だけが発信元を見分ける手がかりになる
- **`_next` は言語ごとに変える。** 英語ページから送って日本語のお礼ページに飛ぶのは不親切
- **`name` 属性は日本語のままでよい**（受信メールの項目名になるだけ）。
  英語ページからの送信も同じ `name` にしておくと、受信側で見比べやすい
- 既存の `privacyPage.contactLink` /「フォームを開く」系のキーは不要になる。
  **消さずに残す**（他所から参照されていないかを確認してから整理する）

### 3-4. 送信後ページ `/contact-received`・`/en/contact-received` を作る

`_next` の飛び先。**存在しないと送信後に404**になるので必ず作る。

- `scripts/prerender.js` で **ja / en の2枚**を出す
- `src/App.jsx` のルートにも ja / en 両方を足す（直接開かれても表示できるように）
- `scripts/generate-sitemap.js` にも追加する
- `hreflang`（ja / en / x-default）と `canonical` を、既存の `/privacy` と同じ形で入れる
- **中身は薄くしないこと。** 「送信されました」の1行だけだと、
  「有用性の低いコンテンツ」を自分から増やすことになる。
  受け取った後どうするか（誤りの指摘は公式ソースを見てから直す、返信の目安、
  アドレス未記入なら返信は無いが内容は読む 等）を書く
- 共通フッター（`components/SiteFooter.jsx`）から辿れるようにするかは任意。
  少なくとも `/privacy` からは相互リンクさせて行き止まりにしない

### 3-5. `frontend/src/styles.css`

`.contact-form` のスタイルを追加。japan-proxy-cost の `src/styles.css` の
`/* お問い合わせフォーム */` ブロックをそのまま流用してよい。

---

## 4. 確認手順

1. `npm run build` が通る
2. **`dist/privacy.html` と `dist/en/privacy.html` を直接 grep して `<form` が入っていること**
   （プリレンダ側の抜けを検出）
3. ブラウザで `/privacy` を開き、**Reactが載った後もフォームが消えていないこと**（ここが本命）
4. `dist` 内の全HTMLに `docs.google.com/forms` が1件も残っていないこと
5. `/contact-received` と `/en/contact-received` が200で開くこと
6. **日英それぞれから1通ずつ送信**し、Gmailに届くこと。
   件名が「池袋ロッカーアプリ — お問い合わせ」であること。
   送信後に**それぞれ対応する言語**のお礼ページへ飛ぶこと
7. **HTMLに `kakuniadao7@gmail.com` が出ていないこと**（エイリアスを使う目的そのもの）
8. sitemap.xml に新ページが入っていること

---

## 5. やってはいけないこと

- **FormSubmitの有効化リクエストを送らない**（§2参照。japan-proxy-cost が止まる）
- **ファイルを直接削除しない。** 不要になったファイルは `削除用フォルダ/` に移動する
  （リポジトリ直下のCLAUDE.mdのルール）
- **`_subject` を他アプリと同じにしない**
- **Googleフォーム本体を削除しない。** 駅前スコアと共用しているため、削除は駅前スコア側の
  引き継ぎ書（§6）の手順に従う。**両アプリの移行完了後**にユーザーが行う

---

## 6. 関連

- 実装の見本: `海外向けアフィリエイトプロジェクト/japan-proxy-cost/src/lib/pages.mjs` の
  `/contact` ページ（`CONTACT_FORM_ENDPOINT` で検索）
- 同じ移行の駅前スコア版（Googleフォーム本体の削除手順もこちら）:
  `駅周辺施設検索アプリプロジェクト/eki-facility-app/お問い合わせフォーム移行_引き継ぎ.md`
