# businessHours.js を退役させた理由（2026-08-23）

`src/i18n/lockerText.js` に統合した。

旧実装は「終日利用可」1語だけを翻訳していたが、`backend/data/lockers.json` の実データに
その値は1件も無く、実際に入っていたのは「初電～終電」668件・「不明」121件だった。
つまり営業時間は英語ページでもほぼ全件が日本語のまま出力されていた。

統合先ではロッカー名・所在地の英訳も併せて扱う。
